# AgentOS

Marketing site for [agentos.homes](https://agentos.homes): the real-estate operating system for professionals.

## Local

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Static output lands in `dist/`. Netlify settings live in `netlify.toml`.
